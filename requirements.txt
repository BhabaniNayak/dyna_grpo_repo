# GPU pod stack — pinned for reproducibility.
# Tested with: NVIDIA driver 570.x (CUDA 12.8), Python 3.11, A100-80G.
# torch is installed separately from the cu124 index — see README quick-start.

torch==2.5.1
torchvision==0.20.1

transformers>=4.51,<4.55     # Qwen3 support requires >= 4.51
tokenizers>=0.21
accelerate>=1.4
peft>=0.14,<0.17
bitsandbytes==0.44.1
datasets==3.1.0
huggingface_hub>=0.26.5
vllm>=0.8,<0.10              # vllm 0.8+ matches transformers 4.51 for Qwen3
trl>=0.16,<0.20

sentencepiece==0.2.0
einops==0.8.0
numpy<2.0
scipy
scikit-learn
pandas<2.3
matplotlib
seaborn
tqdm
wandb
ddgs
sympy==1.13.3
tenacity
jupyterlab
ipywidgets
nltk
sacrebleu
hf_transfer
